#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void){
	int cont2, *vetor5, *vetor1;
	printf("testando\n");
	printf("testando saporra tudo\n");
	/*srand((unsigned)time(NULL));
	vetor5=malloc(sizeof(int) * 10);
	vetor1=malloc(sizeof(int) * 10);
		for(cont2=0;cont2<10;cont2++){
			vetor5[cont2]=rand()%10;
		}
		for(cont2=0;cont2<10;cont2++){
			vetor1[cont2]=rand()%10;
		}

		for(cont2=0;cont2<10;cont2++){
			printf("%d",vetor5[cont2]);
		}
		printf("\n");
		for(cont2=0;cont2<10;cont2++){
			printf("%d",vetor1[cont2]);
		}
		free(vetor1);
		free(vetor5);
		*/
		cont2=100000000;
		printf("%d\n",cont2 );
		return 0;
}